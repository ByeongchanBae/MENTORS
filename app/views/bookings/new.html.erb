  <% if current_user == @mentor.user %>
  <p>You can't book yourself</p>
<% else %>
  <%= simple_form_for [@mentor, @booking] do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date%>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>
<% end %>


  <%= link_to 'Back', mentors_path, class:"btn btn-secondary" %>
